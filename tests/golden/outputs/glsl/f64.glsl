#version 450 core
precision highp float;
precision highp int;

 float global_0 = 1;
 float global_1 = 0.0f;

float f(float x) {
    float y = (0.0f + 0.0f);
    float z = (y + float(5));
    float w = -(0.0f);
    return (((x + y) + global_1) + 0.0f);
}

void main() {
}

void main() {
    main();
}
